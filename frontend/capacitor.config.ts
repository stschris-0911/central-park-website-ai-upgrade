import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tianshu.centralparknavigator',
  appName: 'NYC Park',
  webDir: 'dist'
};

export default config;
