import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tianshushi.centralparknavigator.dev',
  appName: 'Central Park Navigator',
  webDir: 'dist'
};

export default config;
